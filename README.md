# 7 digits codegen for microcontrollers

Tool for generating predefined constants for 7-segment led screens.

Use `7-digits.pl --help` for help.

`symbols` contents predefined segments in alphabetical notation.
